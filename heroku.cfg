[buildout]
extends = buildout.cfg
relative-paths = true
parts =
    pyramid
    test
    coverage-test
    coverage-report
#download-cache = /app/cache/downloads
eggs-directory = /app/cache/eggs
sources-dir = /app/cache/src

[versions]
setuptools = 42.0.2
zc.buildout = 2.13.3
